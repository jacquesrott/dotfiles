[user]
	email = jacques.rott@gmail.com
	name = Jacques Rott
[push]
	default = simple
[pager]
	diff = diff-so-fancy | less --tabs=4 -R
	show = diff-so-fancy | less --tabs=4 -R
[core]
	excludesfile = /Users/jacques/.gitignore
